const show_all = 'show_all';
const show_complete = 'show_complete';
const show_uncomplete = 'show_uncomplete';

const selectFilters = {
    show_all,
    show_complete,
    show_uncomplete
}

export default selectFilters